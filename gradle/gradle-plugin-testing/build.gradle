group 'com.example'
version '1.0-SNAPSHOT'

apply plugin: 'groovy'
apply plugin: 'com.example.plugin'

repositories {
    jcenter()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.4.5'
}

dinnerParty {
    wineName = 'Merlot'
    cheeseName = 'Brie'
}
